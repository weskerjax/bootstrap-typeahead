<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="12.0" DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <Configuration Condition=" '$(Configuration)' == '' ">Debug</Configuration>
    <Platform Condition=" '$(Platform)' == '' ">AnyCPU</Platform>
    <ProjectGuid>{29A6A3E3-70E1-400C-9935-4B90958A637C}</ProjectGuid>
	<OutputPath>bin\</OutputPath>
    <!-- NuGet Package 需要此變數，給一個不存在的敷衍過去 -->
    <TargetPath>.\none</TargetPath>
  </PropertyGroup>
  <ItemGroup>
    <Content Include="Scripts\bootstrap-typeahead-1.0.0\bootstrap-typeahead.js" />
  </ItemGroup>
  <ItemGroup>
    <None Include="bootstrap-typeahead.nuspec" />
  </ItemGroup>
  <Import Project="$(MSBuildToolsPath)\Microsoft.CSharp.targets" />
  
  <!-- 空的 Build -->
  <Target Name="Build" />
  <Target Name="Rebuild" />
</Project>